# Deutsch Tools — TODO list

_Обновлено: 17 сентября 2026_

---

## 📐 Единая сетка

### Ширина контента — ✅ сделано 17 сентября

Заведены три токена в `theme.css`, выбор по содержимому, а не по размеру экрана:

```
--w-wide:    1440px;   сетки карточек, таблицы справочника
--w-content:  900px;   страницы-обёртки (главная index.html и тренажёра)
--w-read:     760px;   сплошной текст (вопрос, результаты, карточка правила)
```

- `S.container` в тренажёре разобран на три (`container` / `containerWide` / `containerRead`) и разведён по 18 местам
- В справочнике ограничены `.panel > .section`, а не сам `.panel`: у панели свой фон, и при сужении посреди страницы оказалась бы центральная плита чужого цвета
- **Шапка вынесена из контейнера** — `TopBar` во всех 14 функциях `render*` поднят на уровень `S.page`, `S.topBar` получил `maxWidth: var(--w-wide)` и `margin: "0 auto 16px"`. Без этого она ездила вслед за контентом: 760 на экране вопроса, 1440 в списке правил, 900 на главной. Боковые отступы приходят от `S.page` (`padding: 20px 16px 60px`), поэтому на телефоне к краям ничего не прилипло, а `max-width` на узком экране не срабатывает вообще — это потолок, а не фиксированная ширина

### Брейкпоинты — сейчас пять разных значений

| файл | брейкпоинты |
|---|---|
| `theme.css` | 560 |
| `index.html` | 700, 600, 420 |
| `cheatsheet.html` | 768 (семь раз) |
| `trainer.html` | 600, 420 |

Страницы перестраиваются в разные моменты: открыл справочник — он уже мобильный, перешёл в тренажёр — ещё десктопный.

**Предложение:** свести к двум — `max-width: 768px` и `max-width: 480px`. Макетные артборды 1920 / 1440 / 768 / 360 — это размеры устройств, а не брейкпоинты; 1920 и 1440 делить не нужно, на обоих одна раскладка, разницу съедает ограничение ширины контента.

Тонкость: `max-width: 768px` срабатывает включительно, устройство ровно 768 получит планшетную раскладку. Если 768 должен оставаться планшетом — писать `767px`.

---

## 🩹 Мелочи

- ✅ **`--scrollbar-thumb` объявлен** — `var(--grey-300)` в `:root`, `var(--grey-700)` в `.theme-dark`. Не `--border-soft`: он `#e5e5e5` на фоне `#f5f5f5`, ползунка не видно
- ✅ **`.rule-seealso` переведён на `var(--accent)`** — был сырой `--green-300`, в светлой теме ссылка «см. также» выходила бледно-салатовой
- ✅ **`rule-render.js` попал под сброс кэша** — `pre-commit` переписан 17 сентября: перечисление файлов убрано совсем. Триггер — любой `.css`/`.js` в коммите (кроме `changelog.js`, у него своя схема с `Date.now()`), подстановка — любой `.css`/`.js` во всех html, список html берётся из `git ls-files '*.html'`. Регистрировать новые файлы больше не нужно. Хук лежит и в репозитории, и в `.git/hooks/`. Ключ `20260914` подтянется к общей дате на ближайшем коммите, где задет любой ассет
- ❌ **`@import` продублирован** — `theme.css` строки 6 и 7, одна и та же строка со шрифтом Orbitron дважды
- ❌ **Синяя обводка при тапе на телефоне** — `trainer.html` строка 54: `input:focus, button:focus` → `input:focus-visible, button:focus-visible`. Кольцо нужно для навигации с клавиатуры, но при тапе оно лишнее; `:focus-visible` различает эти случаи сам
- ❌ **`100vh` → `100dvh`** — `trainer.html` (2 вхождения), `index.html` (4), `cheatsheet.html` (1). В Safari на iOS `100vh` считается без учёта адресной строки: она прячется при скролле, вьюпорт меняется, а `100vh` — нет, страница подпрыгивает. `100dvh` пересчитывается вместе с ней. Для страховки две строки подряд: сначала `100vh`, потом `100dvh`

---

## 🆕 Свежее — 17 сентября

- ❌ **Уровень 5 — рендерер свободного ввода.** Вопросы 5-го уровня (перевод предложения на немецкий вручную) пишутся в базу, но показать их нечем: тип `open` в `trainer.html` не отрисовывается, такой вопрос даёт пустой экран. Сейчас это безопасно — тренажёр фильтрует `q.type !== "open"` во всех местах отбора, и вопросы просто не попадают в пул. То есть база наполняется, а уровень остаётся выключенным до реализации.

  **Что нужно:** обработчик `open` в `renderQuiz` — поле ввода, кнопка проверки, сравнение с `answer` и `altAns`. Открытый вопрос — как сверять: точное совпадение после нормализации (регистр, лишние пробелы, финальная точка) слишком строго для предложения из шести слов, а нестрогое сравнение пропустит ошибку в падеже, ради которой вопрос и задавался. Вариант — сверять пословно и подсвечивать разошедшиеся слова, оставляя решение за учеником. Валидацию через LanguageTool API уже обсуждали для теста на союзы, здесь она же

- ❌ **Союзы: нет своего дома и нет половины переводов.**

  **Первое — раскиданы по шести местам.** Отдельного топика нет, ближайшее `grammar:particles` в `terms`, но «частицы» это другая часть речи, название врёт:

  | лист | топик | что лежит |
  |---|---|---|
  | `terms` | `grammar:particles` | als, deswegen, obwohl, trotzdem, weil |
  | `adverbs` | `communication:agreement` | aber, außerdem, deshalb, sonst |
  | `adverbs` | `movement:direction` | da |
  | `adverbs` | `time:chronology` | seitdem |
  | `adverbs` | `time:frequency` | dann |
  | `phrases` | `communication:text` | bzw. |

  Свести в один топик (`grammar:conjunctions`) или на отдельную страницу — решено позже. Это не вставка строк, а перенос между листами: меняются id, ломаются ссылки. `indem` пока положен в `grammar:particles` как временное место.

  **Второе, важнее — 22 союза встречаются в правилах и отсутствуют в словаре вообще.** Ученик читает `r0053`, видит `dass`, идёт смотреть перевод — такой записи нет:

  `und` (7 правил), `dass` (7), `wenn` (7), `damit` (6), `während` (4), `oder` (3), `ob` (3), `ohne` (3), `sondern` (2), `bevor` (2), `nachdem` (2), `daher` (2), `darum` (2), `danach` (2), `noch` (2), `anstatt` (2), `statt` (2), `denn`, `bis`, `entweder`, `weder`, `sowohl`

  Из 35 союзов, встречающихся в правилах, покрыто словарём только 13. Это базовые слова A1–A2, их отсутствие заметнее любой редкой лексики.

- ❌ **Управление глаголов: предлог + падеж.** Показывать на карточке строкой вида `sich ärgern über + Akk.`, `sich interessieren für + Akk.` Это половина ошибок на письме и целый пласт вопросов, которые сейчас невозможно сгенерировать.

  **Проверено 17 сентября — информация в базе частично есть, но пользоваться ей нельзя.** Она размазана по трём полям в пяти написаниях и покрывает 4% глаголов:

  | где лежит | сколько глаголов | пример |
  |---|---|---|
  | `note` — предлог + падеж | 19 из 487 | `sich ärgern über + Akk`, `warnen vor + Dat` |
  | `label` — то же, в своём формате | 9 | `zeigen + Dat+Akk`, `beantworten + Akk` |
  | `case` — падеж прямого дополнения, не предлог | 37 | `Dativ`, `Akkusativ`, `Dativ+Akkusativ` |

  - Написание падежа в `note` разнобой: `+ Akk` (17), `+ Dativ` (21), `+ Akkusativ` (16), `+ Dat` (4), `+Dat` без пробела (1)
  - `case` тоже: `Dat`, `Dativ`, `Akkusativ`, `Dativ+Akkusativ`, `-`
  - `label` у 44 глаголов, но только у 9 это управление — у остальных 35 там просто копия самого глагола. Поле используется непоследовательно
  - Твой первый пример **`sich ärgern über + Akk` в базе есть** (в `note` у `ärgern`). Второй — **`sich interessieren für + Akk` — нет**: у `interessieren` стоит только `reflexive: true`, ни предлога, ни примечания
  - Из 19 глаголов с управлением в `note` возвратных только 8 — то есть это не отдельная история про `sich`, а общая про управление

  **Вывод:** рендерить на карточку из `note` нельзя — там свободный текст, который заодно содержит примеры и пояснения через `|`. Нужна структурная колонка (`prep` + `prep_case`), перенос в неё того, что уже написано в `note`/`label`, приведение `case` к одному написанию, и только потом рендер. Заполнять придётся с нуля почти весь список: 468 глаголов из 487 без управления
- ❌ **Genitiv у глаголов** — дописать. Перекликается с «Genitiv (Ур.3)» в разделе «Тренажёр»: там решено закрывать Genitiv предлогами и притяжательным, а не глаголами — сверить, не противоречит ли
- ❌ **Показывать примеры употребления слов** — `example_de` / `example_ru` в базе есть (194 записи), но на сайте не видны нигде: ни в справочнике, ни в карточке слова
- ❌ **Альтернативный перевод в режиме «Учить»** — `altRu` на карточке изучения не показывается
- ❌ **Нажатие не успевает отыграть до смены экрана — отзыва нет.** `trainer.html` строки 59–67: `button:active { transform: scale(0.96) }`, `.dt-tap-card:active { transform: scale(0.98) }`, переход `0.15s`.

  **Причина.** `:active` — состояние удержания, а не срабатывания. Мышью клик длится ~50 мс, то есть из 150 мс анимации проходит треть. Дальше React меняет фазу, карточка размонтируется, анимация обрывается на середине. Возврат в исходное состояние пользователь не видит вообще. Увеличивать длительность бессмысленно — удержание от этого длиннее не станет, отыграет ещё меньшая доля.

  **Решение (согласовано).** Разнести на две фазы и наложить на появление нового экрана:

  | фаза | событие | что происходит | длительность |
  |---|---|---|---|
  | вжатие | `pointerdown` | `scale(0.96)` | 60–80 мс, резко |
  | возврат | `pointerup` | обратно в `scale(1)` | 100–120 мс, плавно |
  | переход | ~60% возврата (≈70 мс) | смена экрана, новый проявляется поверх | — |

  - Вжатие ставится руками из `onPointerDown`, а не отдаётся `:active`: на Android Chrome браузер придерживает `:active` на ~100 мс, пока решает, тап это или начало скролла, и эта пауза читается как «кнопка мёртвая»
  - Навигация остаётся на `click`. На `pointerdown` её вешать нельзя: ломается отмена (увёл палец — ничего не произошло) и ломается скролл в списке из 67 карточек, где прокрутка начинается с касания карточки
  - Вход резче выхода — это не прихоть: резкое вжатие совпадает с ощущением удара пальца, плавный возврат не даёт кнопке выглядеть дёрганой
  - Возврат и появление нового экрана перекрываются, мёртвой паузы не остаётся. Ждать пользователю ~70 мс — ниже порога, на котором задержка замечается (~100 мс)
  - Механика появления уже написана: `@keyframes card-slide-in` (`trainer.html` строка 45), сейчас применяется только к flip-карточкам

  **Ловушки.** Таймер перед навигацией снимать по `pointercancel` (палец увели) и при размонтировании — иначе переход после того, как передумали, или `setState` на мёртвом компоненте. Не вешать навигацию на `transitionend`: оно срабатывает отдельно на каждое анимируемое свойство и при прерывании может не прийти вовсе; фиксированный таймер под длительность предсказуемее.

- ✅ **Кнопка «Наверх» в тренажёре** — сделана. Стили перенесены из `cheatsheet.html` в `theme.css` (нужны обеим страницам), починен битый `transition` (средний сегмент `filter 0.2s opacity 0.2s` был невалиден и отключал все переходы разом), добавлено спрятанное состояние и класс `.is-visible`. Появляется после прокрутки примерно на один экран — порог `window.innerHeight`, читается из окна, а не задан числом. В тренажёре монтируется один раз на всё приложение, а не привязана к фазе: на коротких экранах прокрутки нет, значит и кнопки не будет. Скрыта, пока открыт оверлей правила: там `overflow: hidden` на `body`, прокручивается сам оверлей, слушатель окна молчит, и кнопка вела бы себя как мёртвая
- ✅ **Сортировка в справочнике починена** (было: работал только уровень). Добавлен хелпер `topicLabel()`, порядок секций сортируется по немецкой подписи темы с разворотом для `-za`; для режима «по дате» тема встаёт по своему самому свежему слову. Разбор причины ниже, для истории.

  **Причина была такая.** В `renderSections` ветка `level-*` реально перестраивает страницу, а остальные четыре режима падают в общую ветку, где секции выводятся как `Object.keys(groups)` — порядок первого появления темы в `VOCAB`, он никогда не сортируется и не разворачивается. Следствия:
  - `topic-az` и `topic-za` дают одинаковый порядок секций, разворачиваются только строки внутри таблицы (по немецкому написанию)
  - `date-asc` / `date-desc` сортируют строки по числовому хвосту `id`, но внутри секции — увидеть «сначала новые» невозможно
  - Чинится сортировкой `topicsInTab` по немецкой подписи темы из `TOPIC_TITLES` (по подписи, а не по ключу `domen:group` — на экране видна подпись), с разворотом для `-za`
  - **Открытый вопрос по дате:** сортировать секции по самому свежему слову внутри, или для режима «по дате» вообще не делить на темы и выдавать сплошным списком. Второе честнее, но больше работы

---

## 🧩 Режим «Правила» — сделано 16–17 сентября

- ✅ **Отдельный режим «Правила» в тренажёре** — третья карточка на главной. Список всех 67 правил по темам, «Тест по всем правилам» (30 вопросов, round-robin по правилам) и тест по каждому правилу отдельно
- ✅ **`rule-render.js`** — рендер карточки правила вынесен из `cheatsheet.html` в общий файл, обе страницы рисуют правило одним кодом. Туда же переехали `MIN_Q_FOR_TEST`, `RULE_Q_COUNT`, `ruleHasTest()`, `NOTE_ICON_MAP`
- ✅ **CSS карточки правила перенесён в `theme.css`** — вместе с `.g-m/.g-f/.g-n/.g-pl` и `.table-wrap`. Снят класс `rule-box` с карточки: он конфликтовал с `.rule-card` и держался только на порядке строк в файле
- ✅ **Связка справочник ↔ тренажёр** — кнопка «перейти к тесту» на карточке правила активна у правил с готовым тестом и ведёт в `trainer.html?rule=rXXXX`, сразу на первый вопрос. Переделана в капсулу по образцу кнопки «Открыть» с главной: иконка мишени, ролл текста, кружок со стрелкой
- ✅ **Оверлей правила внутри теста** — открывается плоской капсулой «Посмотреть правило» под карточкой вопроса (иконка `eye`), прогресс не сбрасывается. Название правила в шапке стало обычным текстом: раньше это была тонкая цветная строчка, которая читалась как подпись, а не как кнопка. В разборе результатов каждый вопрос с `rule` ведёт к своей теории (во всех режимах, не только в «Правилах»)
- ✅ **Промежуточный экран правила убран** — клик по правилу в списке сразу запускает тест. Экран был пустой и стоил лишний клик
- ✅ **Колонки `difficulty` и `rule` в листе `questions`** — сложность отвязана от типа вопроса, вопрос привязывается к правилу
- ✅ **Порог `MIN_Q_FOR_TEST = 3`** — правило открывается, только если у него набрано не меньше трёх вопросов. Тест из одного вопроса открывался и сразу показывал «1/1, 100%»

### Хвосты режима

- ✅ **126 вопросов вставлены** — в `data.js` 233 вопроса, `q0108`–`q0233`, дублей id нет, разрывов нумерации нет. Правил открыто 33, приглушено 34. По `r0053` теперь 8 вопросов вместо одного
- ❌ **34 правила без теста** — список с количеством недостающих вопросов в `правила-без-тестов.tsv`. Семь открываются малой кровью: `r0005`, `r0006`, `r0018` не хватает одного вопроса, `r0003`, `r0016`, `r0017`, `r0022` — двух
- ❌ **Тип `open` не рендерится** — см. «Уровень 5» выше. Два таких вопроса (`q0106`, `q0107` у `r0001`) сейчас отфильтрованы
- ✅ **Ручные вопросы попадают в блоки и идут раньше авто** — добавлен хелпер `manualMatchesBlock(q, blockId, topics)` в пяти местах отбора, `trainerTopic` остался только внутри него. Приоритет сделан в `applyQuota30` и `buildTestSession`: внутри уровня сложности сначала берутся все ручные, остаток добирается авто. Из 233 вопросов 188 доходят через режим «Правила», 16 через блоки, 8 обоими путями
- ❌ **29 вопросов не видно нигде** — ни в блоках (тема не совпадает), ни в правилах (`rule` пуст). Из них 19 просто неверно размечены (`«От какого слова образовано herzlich?»` помечен `grammar:articles`) — правится в xlsx. Оставшиеся 10 про числа и произношение: в тренажёре нет блока Zahlen, а блок Aussprache из списка блоков отфильтрован
- ❌ **Пре-рендер вопросов** — сейчас пул собирается в браузере на каждый запуск теста. С ростом банка это станет заметно

---

## ⏸ Оборвалось на полуслове — начать отсюда

- ✅ **Правило `r0067` «Условие без wenn: Sollte …, müsste …»** — вставлено, в `data.js` есть (проверено 17 сентября). Сверено по grammis: uneingeleiteter Konditionalsatz, «bei potenzialer Verwendung wird häufig sollte- hinzugesetzt»
- ✅ **Дополнение в `r0059`** — вставлено, `nicht nur` в `content_md` есть (проверено 17 сентября). Смысл дополнения — контраст: с `nicht` союз заменяет, с `nicht nur … auch` добавляет
- ✅ **Английский слой** — закрыт иначе, чем планировалось: вместо отдельного `batch8` английский пересобран целиком для 67 правил в один файл `docs/en-fix/translations-rules-batch01.json` (96 КБ). Инварианты (число строк и `|` в каждой строке) проверены машинно
- ❌ **20 слов на добавление** — список в переписке. Семнадцать разложить по листам после проверки дублей (`Verkehr`, `Meinung`, `Erfahrung`, `wahrscheinlich` в базе точно есть). Три требуют уточнения у Ра:
  - `gelichen` — это `geliehen` (P2 от leihen) или `verglichen`?
  - `esgibt` — это `es gibt`? Уже покрыто примечанием к `geben`, отдельная запись не нужна
  - `beilneh` — обрубок: `teilnehmen` или `beinhalten`?
- ❌ **Прогон после вставки** — `reports/en_case_review.md` должен схлопнуться в «записей нет», строка `⚠ регистр и формы` из консоли исчезнуть. Если не схлопнулся — смотреть, что не попало под `source: manual`

---

## 📦 Партия слов 17 сентября — отдана, ждёт вставки

Прислано 186 строк с урока. Разбор: 14 повторов внутри списка (почти все — одно слово в разных написаниях: `Wortanfag`/`Wantanfag`, `verziehten`/`verzichten`, `Kledeornung`/`Kleideornung`), 69 уже были в базе, **96 новых**.

Отдано пачками в чат, к вставке вручную:

| лист | сколько | id |
|---|---|---|
| `nouns` | 55 | `n1317`–`n1371` |
| `verbs` | 15 | `v0488`–`v0502` |
| `adjectives` | 19 | `a0309`–`a0327` |
| `adverbs` | 6 | `adv0171`–`adv0176` |
| `phrases` | 15 | `ph0109`–`ph0123` |
| `terms` | 1 | `t0059` (`indem`) |

- Новый топик `society:ecology` — `Solaranlage`, `Abgas`, `Überschwemmung`, `Brennstoff`. Пластик и пфанд туда сознательно не пошли, они уже в базе в `shopping:*`
- Новые топики `communication:opinion` (6 фраз) и `communication:discussion` (9 фраз) — Redemittel для ведения обсуждения. Колонка `context: Redemittel` сохранена, как у восьми существующих в `study:classroom`
- `bzw.` выкинут — уже есть в `phrases` как `communication:text`
- Правило Passiv с картинки заводить не нужно — уже есть `r0066 «Passiv – werden + Partizip II»`, B1
- ❌ **Три слова ждут расшифровки от Ра:** `ausgebeu` (это `ausgebucht`?), `verschinken` (`verschicken` или второе написание `verschenken`?), `büchen` (`buchen`? тогда дубль `v0263`)
- ❌ **Redemittel в тесты** — 4-й уровень плитками и 5-й свободным вводом. Пятый упирается в рендерер `open`

---

## 🔴 Сделать первым

- ❌ **Impressum** — висит приоритетом №1 с самого начала. §5 TMG, обязателен для публичного сайта, типовой повод для Abmahnung. Генераторы: e-recht24.de. Проверено 17 сентября: слова `Impressum` нет ни в одном html
- ❌ **Атрибуция DeepL** — через него прогнан весь контент, условия бесплатного тарифа так и не проверены. Возможна обязательная плашка «Powered by DeepL». Проверено 17 сентября: `DeepL` встречается только в комментариях кода, на страницах ничего
- ❌ **README** — не трогался с 4 августа. Не описаны: английский слой через `docs/en-fix`, колонка `no_ru_forms`, поле `examples` у правил, сортировка в справочнике, режим «Правила» и `rule-render.js`. Разделы «Что уже сделано / В работе / Дальше» устарели целиком

---

## 🧹 Гигиена репозитория

- 🔄 **`reports/` лежит в `.gitignore`** — `docs/en-fix/` уже нет (проверено 17 сентября: в игноре только `.env`, `__pycache__/`, `*.pyc`, `*.bak`, `reports/`, `.claude/`, `.vscode/`). По отчётам решить: вынуть из игнора или сознательно оставить локальными
- ❌ **Бэкапы `skip-backup.json` и `translations-backup.json`** лежали в домашней папке временной линукс-машины Cowork — она умирает вместе с сессией, то есть их уже нет. Если понадобится откат — только через историю гита
- ✅ **Семь батчей правил слиты в один** — `translations-rules-batch01.json`, 67 правил, старые батчи 2–9 перенесены в `docs/backup/`. Заодно снята мина с сортировки имён: файлы читаются `sorted()`, то есть лексикографически, и первый же `batch10` встал бы между `batch1` и `batch2` и молча затёр свежее старым. Имя с нулём это исключает
- ❌ **28 правил переводит DeepL, качество не проверялось** — покрыто ручными батчами 38 из 66. Инварианты они проходят, но смысл никто не читал. Тот же аудит по словарю нашёл `Niveau → tour`
- ❌ **Инварианты не гоняются на ручных переводах** — из-за этого `r0035` и `r0058` месяц лежали сломанными: у первого английская таблица уехала на строку и потеряла шапку, у второго пропал второй абзац примечания. Обе починены 6 сентября. Гонять проверку и на `manual`, но не отбрасывать запись, а писать в WARN
- ❌ **`update_data.py` режет пробелы только в русском, не в английском** — асимметрия, которая тихо ломает инварианты при следующем прогоне

---

## 📖 Словарь — хвосты после сентябрьских батчей

- ❌ **46 слов получили `level: A2` вслепую** при вставке — уровень в исходном файле отсутствовал. Часть на самом деле A1: `jung`, `klar`, `interessant`, `traurig`, `dumm`, `lieb`, `normal`
- ❌ **`v0205 werden`** — английское примечание испорчено DeepL: «For more information, visit Futur and Passiv» из «вспомогат. Futur и Passiv»
- ❌ **Четыре словарные неточности** из батча:
  - `duschen` помечен `reflexive: true` — стандартно без `sich`
  - `verpassen` переведён «опаздывать на», точнее «пропустить»
  - `total` переведён наречием «совершенно», как прилагательное — «полный»
  - `außerhalb` и `draußen` совпадают и в `ru`, и в `en` («снаружи» / «outside») — для тренажёра это два одинаковых ответа
- ❌ **`Er ist ein Lehrer` в `r0024`** — в примере правила стоит артикль. С профессией после `sein` артикль обычно опускается: `Er ist Lehrer`. Артикль появляется только при определении: `Er ist ein guter Lehrer`. Правило сейчас закрепляет форму, которую на DTZ сочтут неестественной
- ❌ **Колонки `separable` / `prefix` / `reflexive` / `case` заполнены редко** — `separable`/`prefix` у 21% глаголов, `reflexive` у 6%, `case` у 7%. В партии 17 сентября новые глаголы заполнены честно — старые подтягивать отдельной задачей

### Тонкие темы у существительных (7 тем ≤3 слов)

Слова для этих тем в базе есть, но разложены по соседним темам — это дыра в разметке, а не в словаре.

| тема | слов | решение |
|---|---|---|
| `communication:text` | 1 | слить в `communication:docs` |
| `communication:requests` | 1 | слить в `communication:general` |
| `basic:mental` | 3 | слить в `state:abstract` |
| `basic:senses` | 2 | слить в `state:abstract` либо дополнить (Geruch, Duft, Ton, Aussicht) |
| `study:classroom` | 2 | слить в `products:stationery` |
| `study:places` | 2 | слить в `place:buildings` |
| `money:income` | 3 | дополнить: Lohn, Einkommen, Rente, Trinkgeld, Mindestlohn, Zuschlag |

Отдельно: завести пару `products:care` под `Creme`, `Parfüm`, `Zahnpasta`, `Shampoo`, `Seife`. `Zigarette` туда не относится — ей место в `shopping:general`.

---

## 📊 Работа в xlsx — ближайшее

### Скобки в поле `ru` (63 записи)

Уточнение внутри перевода вместо `note`: `рука (ладонь)`, `снимать (жильё)`, `идти (о дожде)`, `кого-то (Akkusativ)`.

| лист | записей |
|---|---|
| nouns | 26 |
| verbs | 23 |
| adjectives | 5 |
| phrases | 3 |
| numbers | 2 |
| adverbs | 2 |
| pronouns | 2 |

### Примеры (`example_de` / `example_ru`)

- **48 немецких примеров без русского перевода.** Существительные 25, прилагательные 13, наречия 10. Английский у них есть, потому что считается с немецкого напрямую, а русского нет
- **Покрытие низкое**: 194 примера на 2090 слов. У фраз, местоимений и числительных колонки нет вообще
- **Три разных имени колонки** под одно и то же: `example_de` в словаре, `example` в звуках, `examples` в правилах

### Формат `examples` у правил

Рендер на сайт сделан, но старые ячейки к единому виду не приведены. Из 67 правил `examples` заполнены у 24:

- 15 в правильном формате — построчно `немецкое – перевод`
- 6 через вертикальную черту: `r0024`, `r0025`, `r0026`, `r0028`, `r0029`, `r0031`
- 1 markdown-таблицей с `||`: `r0027`
- 1 одной строкой: `r0023`

Плюс треть дублирует само правило: `r0029` — четыре примера из семи слово в слово повторяют таблицу, `r0031` — три из пяти, `r0057` — три из четырёх.

### Колонки, которых не хватает

| лист | alt-de | alt-ru |
|---|---|---|
| verbs | нет | да |
| adjectives | нет | да |
| numbers | да | нет |
| terms | нет | нет |

Упрётся при следующем добавлении слов.

### Перекладка TERMS

В одной таблице три разные сущности: 25 грамматических терминов, 15 вопросительных слов, 13 предлогов.

- Вопросительные и предлоги (28 записей) перенести в `vocab`
- Уберёт 5 дублей: `neben`, `vor`, `um`, `am`, `der Singular`
- У предлогов в поле перевода стоит название падежа (`Dativ`) вместо перевода
- Требует правок кода: вкладка «Begriffe» перестаёт их показывать, словарь начинает

### Пометка регистра

Отдельным полем (`разг.`, `сленг`, `груб.`) вместо свободного текста в `note`. В словаре есть сленг и ругательства, англоязычному регистр сейчас не виден.

### Дубль `no_ru_forms`

В `data.js` для одних и тех же 12 глаголов пишутся два ключа — `noRuForms` (схемное поле) и `ruNoForms` (из `VOCAB_KEYS`). Безвредно, но лишнее.

---

## ✅ Английская версия — закрыто

Перевод шёл цепочкой `DE → RU → EN`. Английская версия была переводом русского конспекта, а не немецкого оригинала.

- ✅ **A** — английский контент спрятан за флагом `window.I18N_CONTENT_EN`
- ✅ **B** — VOCAB на источник `de`, DeepL `DE→EN` + `context`, обратный перевод как детектор, арбитр на Haiku. 574 флага: 374 ok, 185 fix, 15 skip. `Eintritt: Login → entrance`, `backen: oven → to bake`, `Wäsche: underwear → laundry`
- ✅ **C** — RULES: 31 исключение вручную, 28 через ignore-теги. Найдена поломка `tag_handling=xml` на многострочном вводе, решение построчный перевод
- ✅ **D** — ignore-теги для `note` и `warning`: 303/304 и 14/14
- ✅ **E** — TERMS и SOUNDS выведены из автоперевода. `regelmäßig: transitive verb → regular verb`, IPA вместо кириллицы. Добавлены `comboEn` / `exampleEn`
- ✅ **F** — `translate_changelog` под ignore-тегами. 14 из 23 записей теряли немецкое. Введён `manualEn`
- ✅ **G** — `altEn` на новый кэш, удалён последний легаси-механизм (`_parse_old_array_by_id`)
- ✅ **H** — 12 ручных переводов, `alt-de` / `alt-ru` в схему, предупреждение о дублях заголовков
- ✅ **Ветка `en-fix/stage-h` смержена в `main`** (проверено 5 сентября)
- ✅ `examplesEn` добавлен в цикл перевода правил и в отчёт
- ✅ batch6 — 26 правил переведены вручную, вместе с примерами
- ✅ batch7 — `r0065` (Futur) и `r0066` (Passiv)
- ✅ **Пересборка английского для 64 правил** — один файл `batch01` вместо девяти, инварианты проверены машинно

### Хвосты по переводу
- ❌ Косметика changelog: `«Работе» → Raba`, `«Общество» → Company`, `«Союзы» → Links`, `«с Dativ» → both from Dativ`. Правится руками + `manualEn: true`
- ❌ Ревизия 266 записей `altEn` с пометкой `deepl-ru` — немецкого источника нет, пивот остаётся
- ❌ Решить судьбу арбитра на Haiku: вырезать или оставить
- ❌ Удалить ветки `en-fix/stage-a`, `en-fix/stage-b`, `en-fix/stage-c` — единственные из семейства, не влитые в `main`

---

## 🎨 Вёрстка

- ❌ **Убрать внешний контейнер `.section`** — три уровня вложенности, внешний ничего не несёт и зажимает контент на телефоне
- ❌ **TERMS с таблицы на карточки** — по образцу спряжений: заголовок `термин — перевод`, полоса слева, служебное внизу за разделителем
- ❌ Цветовые метки в карточках: падеж у предлогов, вопрос у падежей. По образцу `haben` / `sein`
- ❌ Заголовок `Partikeln` не соответствует содержимому — там лежат союзы, см. задачу про союзы выше
- ❌ Модалка по клику на подсказку поиска — открывать карточку вместо перехода
- 🔄 **Дублирующийся заголовок `###` в 15 правилах** — первый подзаголовок повторяет название карточки. Рендер его теперь снимает сам (`renderRuleCard`), но в данных дубль остался — чистить при случае
- ✅ **Тёмная тема в карточке правила** — `.rule-card` не задавал `color`, и текст внутри оверлея в тренажёре падал на дефолт браузера (чёрный на чёрном). В `trainer.html` у `body` цвет не задан, в отличие от справочника. Добавлен `color: var(--text-main)` на саму карточку — чинит её в любом окружении
- ✅ **Полоса прокрутки и тап** — `theme.css`: `scrollbar-gutter: stable` (окно перестало менять ширину между длинной и короткой страницей, шапка не дёргается на ~7px), `background: var(--bg-body)` на `html` (иначе жёлоб белый — фон в тренажёре красит внутренний div, до жёлоба не дотягивается), `-webkit-tap-highlight-color: transparent` (серый прямоугольник при тапе на Android; своя обратная связь через `:active` уже есть)
- ❌ **`NOTE_ICON_MAP` — шесть мёртвых маркеров.** В карте восемь эмодзи, реально в правилах встречаются только 💡 и ⚠. У 📍 🔑 ❗ ✗ ✓ 👉 — ноль вхождений

### Сделано в сентябре
- ✅ Карточный редизайн правил (`renderRuleCard`), иконка домена на плашке в шапке
- ✅ Примеры вынесены в отдельный блок с зелёным заголовком и разделителями
- ✅ Сортировка в шапке справочника: тема, уровень, дата добавления — в обе стороны. Чинилась 17 сентября: порядок секций не сортировался ни в одном режиме, кроме уровня
- ✅ Вкладка «Новые» разложена по темам вместо одного списка
- ✅ Составной род `m/f` рисуется двумя бейджами
- ✅ Ссылки «см. также» показывают все связанные правила, а не только первое
- ✅ Служебные поля тренажёра (`ruForms`, `ruNoForms`, `noRuForms`, `meanings`) убраны из карточки слова
- ✅ `Pl:` убрано из колонки перевода у существительных

---

## 🧠 Тренажёр

### Систематизация уровней
- Ур.1 — перевод (все части речи)
- Ур.2 — склонение формы слова: феминитив, множественное, артикль, компаратив, склонение прилагательных, местоимения. Плюс `genL2_topic_classify`
- Ур.3 — склонение в предложении: артикль+прилагательное по падежам (Nom/Akk/Dat/**Genitiv**), таблица спряжения. Perfekt/Präteritum отдельными кнопками
- Ур.4 — составление предложений плитками
- Ур.5 — свободный ввод перевода предложения на немецкий

### Недостающие генераторы
- ❌ Склонение прилагательных (Ур.2) — алгоритмически из рода и падежа, правила есть (`r0033`, `r0040`)
- ❌ Genitiv (Ур.3) — правила есть (`r0047`, `r0051`), тестов нет. Закрывать предлогами `wegen / trotz / während / statt` и притяжательным Genitiv, а не глаголами с Genitiv: их в современном немецком около 14, все B2–C1 и книжно-юридические
- ❌ Уровень 5 — см. отдельную задачу в «Свежее»
- ❌ Прилагательные и наречия обрываются на Ур.2, достроить Ур.3/4
- ❌ Числа не тестируются вообще — блока Zahlen в тренажёре нет
- ❌ Звуки не тестируются — блок Aussprache отфильтрован из списка блоков (`kind: "sounds"`), он только в справочнике
- ❌ Фразы в Ур.4 плитками
- ❌ Отделяемые и возвратные глаголы — сейчас исключены почти везде

### Примеры из словаря как источник заданий

Готовые предложения из `example_de` / `example_ru` в тренажёре не используются вообще, а они живее автогенерации: настоящие фразы, а не собранные по шаблону.

Пул на сегодня — **146 слов**, у которых есть и немецкий пример, и русский перевод: глаголы 61, существительные 54, прилагательные 21, наречия 10. Ещё 48 примеров без русского (см. «Работа в xlsx»), после их перевода пул станет 194.

Куда ложится:
- Ур.1 — показать русский перевод примера, собрать/выбрать немецкий
- Ур.4 — плитки из слов готового предложения, порядок проверяется по оригиналу
- Ур.5 — свободный ввод перевода предложения

Дистракторы для выбора можно брать из примеров слов той же темы. Отдельно решить: примеры длиннее ~8 слов на плитки не годятся — резать или отсеивать.

**Разбор пула (17 сентября).** Выгружены все примеры из базы — 502 уникальные записи: из полей `example` у слов и звуков, из `examples` у правил, из `note` у фраз и терминов. Предложений от четырёх слов — 306, и только **~62 имеют структуру, где порядок слов неочевиден**:

| структура | шт. | тип вопроса |
|---|---|---|
| придаточное (weil, dass, wenn, obwohl…) | 41 | `tiles` |
| Dativ + Akkusativ | 18 | `tiles` |
| модальный + инфинитив | 9 | `tiles` |
| отделяемая приставка | 7 | `tiles` |
| рамка Perfekt / Passiv | 6 | `tiles` |
| инверсия | 4 | `tiles` |
| падеж / предлог | 99 | `fill` |
| плоское | 253 | не брать |
| короткое словосочетание | 65 | не брать |

**Правило отбора: тип вопроса выбирает структура предложения, а не наличие предложения как такового.**

- есть рамка (придаточное, Perfekt, модальный, инверсия, Dat+Akk, отделяемая приставка) → `tiles`
- рамки нет, но есть артикль, предлог или падежная форма → `fill` / `mc` с дыркой на форме
- плоское без падежной соли (`Berlin ist die Hauptstadt Deutschlands`) → не брать: собирается одним способом, вопрос ничего не проверяет

**И главное:** предложение привязывается к правилу **по своей структуре**, а не по слову, из которого взято. `Im Winter schneit es` пришло из статьи «Winter», а тестирует инверсию — значит уходит в `r0053`. Так примеры от слов и фраз, у которых своей привязки нет, становятся материалом для правил.

Рабочие файлы: `примеры-остаток.tsv` (все 502 записи с разметкой по структуре, колонка «уже разобрано» помечает 138 ушедших в первые две партии) и `правила-без-тестов.tsv`.

### Прочее
- ✅ Мини-тест после карточек изучения unregel
- 🔄 Переделка блока «Новые» — дистракторы из новых слов своей группы
- ✅ Автоспряжение русских глаголов — `pres` для несовершенного вида, `futr` для совершенного, колонка `no_ru_forms` для неспрягаемых, помеченные исключены из заданий
- ❌ Тест на союзы, свободный ввод. Три категории по позициям:
  - **Position 0** — сочинительные (aber, und, oder, denn, sondern), порядок не меняют
  - **Position 3** — подчинительные (weil, da, dass, obwohl, wenn, als, während, bevor, nachdem, damit, ob), глагол в конец
  - **Position 2** — наречия-коннекторы (deshalb, deswegen, daher, darum, trotzdem, dann, danach, sonst, außerdem), инверсия
  - Разметить каждый союз полем типа позиции. Валидация через LanguageTool API
  - Упирается в задачу про союзы выше: 22 из них вообще нет в словаре, размечать пока нечего
  - Частично закрыто вопросами по `r0053`, `r0057`, `r0059`, `r0060` — но там выбор из вариантов, а не свободный ввод
- ❌ Автогенерация fill+tiles по 11 шаблонам для существительных
- ❌ Феминитив-вопросы — принимать феминитив как валидный ответ
- ❌ Прогресс пользователя (localStorage)

---

## ⚙️ Инфраструктура

- ❌ Озвучка слов (Piper TTS) — заблокирован huggingface.co в песочнице
- ❌ Pre-build — убрать client-side Babel (React+Babel ~1.5MB с CDN, компиляция в браузере при каждом визите)
- ❌ Демка → PWA (manifest, service worker, кнопка установки, инструкция для iOS)
- ❌ Автоперевод на русский для новых слов — черновик, который правишь вручную. Отложено

---

## ⚖️ Юридическая часть
_(не юрист, общие ориентиры, не консультация)_

- ❌ **Impressum** — см. «Сделать первым»
- ❌ **Атрибуция DeepL** — см. «Сделать первым»
- ❌ Disclaimer: нет аффилиации с telc gGmbH / Goethe-Institut
- ❌ Disclaimer: контент «as is», без гарантии точности
- ❌ Privacy Policy — не горит, пока нет аккаунтов и трекинга
- ❌ Cookie-consent — понадобится вместе с аналитикой
- ❌ Open-core: MIT публичная часть + приватный платный сервис
- ❌ Статус для приёма платежей (Gewerbe / Kleingewerbe)

---

## 💰 Монетизация и продвижение

- ❌ **Пост в LinkedIn** — русские версии обоих постов готовы, не опубликованы
- ❌ Донат: PayPal.me / Ko-fi / Buy Me a Coffee (не Boosty, плохая поддержка европейских банков) → позже freemium
- ❌ Реклама — опционально, только бесплатный уровень, ненавязчиво
- ❌ Аналитика посещений
- 🔄 Telegram-бот «слово дня»:
  - ✅ Скрипты готовы: `bot_listener.py`, `post_quiz.py`, `quiz_data.py`, `explanations.py`, `config.py`, `weekly_stats.py`
  - ❌ Хостинг на Oracle Cloud Free Tier (Frankfurt, Ampere A1.Flex) — «Out of host capacity»
  - ❌ Логотип бота
  - ❌ Название бота

---

## 🎨 Отдельно
- Нейминг проекта — отложен (Sprax, Deutra, Fux, Facha; по Sprax есть шум на Google Play)

---

## 📚 Закрыто ранее
- ✅ **Слэши в поле `ru`** — было 186 записей, в базе сейчас ноль
- ✅ Слэши в `de` разнесены по `de` / `alt-de` (8 записей)
- ✅ Ложное предупреждение `new=TRUE` при существующем переводе — проверка удалена
- ✅ Правила `r0062`–`r0066`: seit/seitdem и bis, вопрос о лице, `wo(r)-` / `da(r)-`, Futur, Passiv
- ✅ Пять новых пар тем: `communication:docs`, `place:size`, `products:medical`, `state:qualities`, `study:language`
- ✅ Баг поиска — результаты уходили под sticky-шапку
- ✅ Кнопка «← Блоки» в тренажёре
- ✅ Кнопки соцсетей (LinkedIn / Behance / Instagram)
- ✅ Редизайн главной страницы
- ✅ Мультиязычность RU/EN — i18n на всех страницах
- ✅ Аудит уровней тестирования
- ✅ `changelog.js` — восстановлена история с 22 мая по 23 августа
- ✅ Баг: блок «Verben» подмешивал существительные
- ✅ `vier → четыри`, `…zehn` / `…zig`, 15 заглавных в русских ячейках
- ✅ `handeln` разделён, `es handelt sich um` в `phrases` (`ph0085`)
- ✅ Пара отрицаний `kein` / `nicht` разведена
- ✅ Таблица притяжательных удалена из словаря (вызывала коллизию `vocab_hash`)
- ✅ **Аудит русского слоя всех 67 правил** — 66 ячеек в 49 правилах исправлены. Среди найденного: `r0057` (после `weder…noch` между главными предложениями `noch` занимает первое место и вызывает инверсию), `r0058` (пример с `erlauben` был переведён с неверным подлежащим)